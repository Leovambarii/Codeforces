#include <bits/stdc++.h>

int main() {
    int t;
    std::cin >> t;
    while(t--) {
        long long a, b, c, d;
        std::cin >> a >> b >> c >> d;
        std::cout << b << " " << c << " " << c << std::endl;
    }

    return 0;
}